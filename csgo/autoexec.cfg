// AUDIO
snd_musicvolume "0"
snd_mute_losefocus "1" // mute game when alt-tabbed

// HUD
cl_righthand "1"

// MISC
con_enable "1"
cl_autowepswitch "0" // auto weapon switch on pickup
cl_use_opens_buy_menu "0" // E opening buy menu
cl_disablehtmlmotd "0" // Disable HTML MOTD
cl_forcepreload "1" // pre-load everything on map startup
cl_disablefreezecam "1" // no need to waste too much time
cl_showloadout "1" // always show equipment on right side

// CROSSHAIR
cl_crosshairalpha "192"
cl_crosshaircolor "5"
cl_crosshaircolor_b "255"
cl_crosshaircolor_r "255"
cl_crosshaircolor_g "255"
cl_crosshairdot "1"
cl_crosshairgap "0"
cl_crosshairsize "3"
cl_crosshairstyle "4"
cl_crosshairusealpha "1"
cl_crosshairthickness "1"
cl_fixedcrosshairgap "0"
cl_crosshair_outlinethickness "0"
cl_crosshair_drawoutline "0"

// MOUSE
m_rawinput "1" // default: 1
m_customaccel "0" // default is 3
m_mouseaccel1 "0" // default
m_mouseaccel2 "0" // default
sensitivity "2.0"
zoom_sensitivity_ratio_mouse "1.0" // default

// ALIASES
alias "+info" "+showscores; net_graph 1"
alias "-info" "-showscores; net_graph 0"
alias "equip_he" "use weapon_knife; use weapon_hegrenade"
alias "equip_flash" "use weapon_knife; use weapon_flashbang"
alias "equip_smoke" "use weapon_knife; use weapon_smokegrenade"

// GENERAL BINDS
unbind i
unbind k
bind "`" "toggleconsole"
bind enter "messagemode"
bind backspace "messagemode2"
bind tab "+info"
bind space "+jump"	// default
bind shift "+speed"	// default
bind ctrl "+duck"	// default
bind w "+forward"	// default
bind a "+moveleft"	// default
bind s "+back"		// default
bind d "+moveright"	// default
bind q "lastinv"	// default
bind e "+use"		// default
bind r "+reload"	// default
bind f "drop"
bind g "+lookatweapon"
bind m "teammenu"	// default
//bind k "+voicerecord" // default
//bind z "radio1" // default
//bind x "radio2" // default
//bind c "radio3" // default
bind z "enemyspot"
bind x "needbackup"
bind c "sectorclear"
bind b "buymenu"	// default
bind "," "buyammo1" // default
bind "." "buyammo2" // default
bind mouse1 "+attack"
bind mouse2 "+attack2"
bind mouse3 "use weapon_knife"
bind mouse4 "+voicerecord"
bind mouse5 "equip_smoke"
bind mwheelup "equip_flash"
bind mwheeldown "equip_he"
//bind mwheeldown "invnext" // default
//bind mwheelup "invprev"   // default
bind f3 "autobuy"	// default
bind f4 "rebuy"		// default
bind f5 "jpeg"		// default (screenshot)
bind f6 "save quick"// default (what?)
bind f7 "load quick"// default (what?)
bind f10 "quit prompt" // default
bind f12 "quit"

// BUY BINDS
bind "kp_pgup" "buy awp"
bind "kp_uparrow" "buy ak47; buy m4a1"
bind "kp_home" "buy galilar; buy famas"
bind "kp_rightarrow" "buy deagle"
bind "kp_ins" "buy vesthelm; buy vest"
bind "kp_del" "buy vesthelm"
//bind "kp_enter" "buy defuser" // kp_enter is broken
bind "kp_plus" "buy defuser"
bind "kp_end" "buy hegrenade"
bind "kp_downarrow" "buy flashbang"
bind "kp_pgdn" "buy smokegrenade"
bind "kp_leftarrow" "buy molotov; buy incgrenade"
bind "kp_5" "buy decoy"
bind "kp_slash" "autobuy"
bind "kp_multiply" "rebuy"
bind "kp_minus" "echo kp_minus"

// RADIO BINDS: http://csgo.gamebanana.com/scripts/7946

// APPLY
host_writeconfig // write settings to config.cfg
echo "*** autoexec.cfg loaded"
